# 11：MCM2004U 保守見積・ユーザー契約一覧

最新 src (3).zip を基準とした既存2ファイルの修正です。追加のアプリファイルはありません。

- CSS：検索欄の間隔をVB配置に近づけ、タイトルのフォント、検索ボタンの色、24pxの行高をWeb画面に合わせました。一覧の各列の幅は維持しています。
- JavaScript：VB Designerの `MultiSelect=False` に合わせ、Ctrl・Shift操作でも選択を1行に限定しました。行削除で意図せず複数行を対象にすることを防ぎます。

このフォルダの `src` を会社側の `src` に重ねてください。08・09・10との同名ファイルの重複はなく、同じ基準srcなら順不同で反映できます。会社側で同じファイルを変更済みの場合は差分一覧の元SHA256を確認して統合してください。

## 確認

サーバー42項目、SQL・受け渡し35項目、ブラウザー25項目が成功。追加でShiftの単一選択と24px行を確認しました。検索条件、日付エラー、0件、破棄・削除の確認とキャンセル、権限、固定列、複数の画面幅、遷移先のID・モードを確認しています。

関連画面は最新版対応の CustomerWorkflowTest でも64項目が成功しています。2004Uのテストは主にRepositoryの模擬データ、関連画面はローカルH2を使用。本番SQL Server・VB実機での動作保証を意味するものではありません。

[VB／Web比較画像](../../検証記録/review_20260914/2004U/2004U_比較.png)（VB側はDesignerからの再構成）

[詳しい確認結果](../../検証記録/review_20260914/2004U/確認結果.md)

次は2003U経由で開く2005Uです。

2026-09-14 再確認：検索欄とボタン帯の背景を白で連続させ、行削除・新規ボタンが白背景の上に載るよう修正しました。サーバー42項目・ブラウザー25項目を再実行し成功。修正後画像も更新済みです。
