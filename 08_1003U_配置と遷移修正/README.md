# 08：MCM1003U 取引先見積・契約一覧

2026-09-14 18:41受領のsrc (3).zipを基準とする差分です。旧04〜07は重ねません。今回の既存3ファイルだけを会社の最新srcの同じ相対パスへ反映し、ビルド・再起動してください。会社側で同じファイルが進んでいる場合は、差分一覧の元SHA256と照合して統合してください。

## 変更

- 検索欄をVBの並び・入力幅に近づけ、ラベルとの間隔を調整。長い納入先名ラベルはWebフォントで収まる幅を確保。
- 検索ボタンを条件欄の右下、新規・行削除を一覧の右上、依頼一覧出力を右下に配置。外周8px・紫タイトル40px・文字18px・通常行24pxへ統一。
- 一覧は画面幅に追従したまま維持。高さ1000pxの検証画面では、一覧領域が513pxから706pxに拡大。
- 1003Uから1004Uを開く際に戻り先を明示。以前3007Uから同じ見積を開いていた場合に、閉じると3007Uへ戻ってしまう不具合を修正。今回の入口で最新データを読み直す。

検索SQL・結果件数・集計・削除処理は変更していません。VBは一覧のMultiSelect=Falseで、現在の単一行選択を維持します。元srcは受領状態のまま保持し、修正はこの差分フォルダに保存しています。

## 検証

- 受領srcの386 Javaファイルをコンパイルし、修正Controllerを再コンパイル。
- 既存1001／1003関連63項目成功。検索・入力チェック・権限・正確なIDによる遷移・破棄・関連19表の削除範囲／ロールバック・Excel出力等。DB検証部分はローカルH2、その他は実ServiceとモックRepositoryを使用。
- 1003→1004の戻り先不具合を実Controllerで再現し、修正後の5項目成功。詳細Serviceはモック。1004全体の保存確認とは別のテスト。
- Chrome操作18項目成功。うち3項目は隣接する1001の既存回帰。確認ダイアログ・キャンセル・行選択・送信ID・参照権限・JSエラーなし。POST受信はローカル検証用。
- Chromeの1024／1264／1440／1920px幅で表示を確認。会社のSQL Server・実ログインを含む本番起動は未検証。

配置比較：[VBとWebの比較画像](../../検証記録/review_20260914/1003U/1003U_比較.png)。左はVB Designerからの再構成で、VB実機のスクリーンショットではありません。右は実Thymeleaf出力をChromeで表示した検証画面です。

検証記録：検証記録/review_20260914/1003U/。担当画面の順番は1003→1004→1007→2004→2005（2005へは2003を経由）。
