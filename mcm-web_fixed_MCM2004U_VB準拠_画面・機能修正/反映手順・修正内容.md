※最新版は19ファイル版です。以前の10／16ファイル版を反映済みでも、このsrc全体を重ねて反映してください。

# MCM2004U 保守見積・ユーザー契約検索：VB準拠の画面・機能修正

対象はメニュー右上の見積関連から開く、今回の「②」の画面です。「③」MCM1003Uは今回変更していません。最新版の `mcm-web/src` を基準にした追加修正です。

## 反映手順

1. このフォルダ内の **src フォルダ全体**を、本番プロジェクトの src がある階層へドラッグ＆ドロップし、フォルダをマージして同名ファイルを上書きしてください。src 自体を削除・置換する操作ではありません。
2. **既存18ファイルの上書きと、新規1ファイルの追加**を行います。新規ファイル `src/main/resources/static/js/mcm2004u.js` も必要です。ファイル名・パッケージ名は既存構成に合わせています。
3. アプリを完全停止し、プロジェクトの通常手順でクリーンビルド・再配置・再起動してください。Javaの修正を含むため、src のコピーだけでは稼働中のアプリに反映されません。
4. 再ログインし、ブラウザーを強制再読み込みして確認してください。

#258・#261・#265 → #257 の反映順序は従来どおりです。#236と今回の19ファイルは、これらの修正ファイルと重複しません。先のパッケージが反映済みなら、今回の src だけを追加反映できます。作業用の `mcm-web/src` は変更していません。

## 変更したファイル

| ファイル | 内容 |
|---|---|
| controller/Mcm2004uController.java | 検索状態保持、権限確認、操作対象確認、再送抑止、各画面への受け渡し |
| service/Mcm2004uService.java | VBメッセージ・日付判定・状態コード、破棄・削除のトランザクション |
| repository/Mcm2004uJdbcRepository.java | 権限照会、更新列修正、VBの関連データ削除処理 |
| form/Mcm2004uForm.java | 初期状態で「見積」「契約」をチェック |
| controller/Mcm2007uController.java | 2004Uの契約作成から渡された見積を読み込む |
| service/Mcm2007uService.java | 指定見積のブランド・期間の初期選択 |
| repository/Mcm2007uRepository.java | 現在期間、次いで直近将来期間の見積を取得 |
| templates/mcm2004u/index.html | 検索条件・一覧・ダイアログの配置 |
| static/css/mcm2004u.css | VB列順・列幅・固定列、他Web画面の色・フォント・タイトル帯 |
| static/js/mcm2004u.js（新規） | 行選択、確認ダイアログ、二重操作防止 |

Javaファイルは `src/main/java/com/daifuku/mcm/` 配下、HTML/CSS/JSは `src/main/resources/` 配下です。2007Uの3ファイルは2004Uの「契約 作成」の受け渡しに必要な変更です。

## 修正内容

- 検索条件のまとまり、一覧21列の順序と幅、左側固定列をVB版に合わせました。長い一覧は横・縦スクロールし、タイトル帯は他Web画面と同じ高さ40px・紫色 `#453f78`、本文12px、見出し18pxです。
- 初期チェック、条件なし・検索0件・日付不正・破棄確認のメッセージをVB版のコードと `CPMessage.xml` に合わせました。検索条件を保持し、エラー時は古い結果を消します。
- 見積複製の遷移先をVBどおり2001Uへ修正しました。参照・修正・契約作成も、遷移先が実際に受け取るDTO／セッションへ渡し、以前開いた別データが残らないようにしています。
- 見積・契約の破棄に使う状態コードと更新監査列を実ソースに合わせました。権限は担当者の機能別権限から判定します。
- メンテナンス利用者の「行削除」は、従来の破棄への書換えから、VBの `MCM_DEL_PAC.SP_UM` に沿った関連データ削除へ修正しています。選択した基本見積と、それに関連する契約・明細が削除対象です。複数行は一つのトランザクションで処理し、途中で失敗した場合は全体を取り消します。
- 古い検索結果からの操作や二重送信を拒否し、処理前にDB上の対象・操作可否を再確認します。

## 確認結果と残る確認

変更Java11ファイルのコンパイルに成功しました。実Controller・Service・Thymeleaf・JavaScriptを使用したローカル検証で、サーバー35項目、SQL／2007U受け渡し33項目、ブラウザー25項目の **合計93項目が成功**しました。1280×720、1366×900、1920×1080で撮影し、固定列の裏から文字が出ないことも確認しました。

DBはモックです。SQLの対象・パラメータ・順序、およびSpringトランザクション経由の取り消しを検証しましたが、**実SQL Serverのテーブル・ビュー・制約・実データでの実行、実環境の権限設定、遷移先画面の一連の登録業務は未確認**です。追加確認では、直接の遷移先5画面の描画と複製後の選定まで確認済みです。反映後は検証環境で、実データ検索、各リンクの対象、権限別表示、破棄・削除の結果を確認してください。

詳細とスクリーンショット：作業ルートの `review_20260908/fix2004/`。`results-1366.png` はテスト用データを表示した画面です。

## 遷移先確認による追加修正（16ファイル版）

前回の10ファイルに、2001UのController・Service・Repository・index.html、2002UのController・index.htmlの6ファイルを追加しました。前回の2004U・2007UのControllerにも異常系の保護を追加しています。

2001U／2002Uの表示例外、複製時の納入先・プラント未取得、選定時のListバインド例外、2002UへのDTO未設定を再現して修正しました。チェックIDは保存済みの検索結果と照合し、納入先・プラントの初期選択／親子表示も整えています。

追加50項目が成功しました（2004U異常系7、遷移統合21、ブラウザー22）。これまでの検証は合計143項目ですが、実DBでの動作保証ではありません。前回の93項目の内訳と今回の結果は、作業ルートの `review_20260908/fix2004/遷移先・行削除の追加確認.md` に記載しています。

この16ファイル版のsrc全体を反映してください。前回の10ファイルを反映済みでも、同じ操作で重ねて反映できます。遷移先画面の登録・帳票等の業務全体は今回の確認対象外です。

## 通常操作・目視確認による追加修正（19ファイル版）

2006Uの項目の重なりと、2006U／2007Uの戻る・キャンセルのURLを修正しました。2004Uからの契約作成キャンセルは検索画面へ戻り、2001U／2002Uにも検索画面への戻るリンクがあります。2006Uのindex.html、2007Uのindex.html・step2.htmlを追加しています。最新の19ファイル全体を反映してください。

通常操作を模したブラウザー確認24項目と画像目視が完了し、確認中のHTTPエラー・JavaScriptエラーは0件でした。実DBの動作確認は未実施です。詳細は review_20260908/fix2004/通常操作・目視の追加確認.md を参照してください。

