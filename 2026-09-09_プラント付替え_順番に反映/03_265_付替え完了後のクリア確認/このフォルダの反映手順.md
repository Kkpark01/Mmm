# 03_265_付替え完了後のクリア確認

このフォルダは全4段階のうちの 03 です。
親フォルダの「最初に読む_反映手順.md」に従い、01 → 02 → 03 → 04 の順で反映してください。

このフォルダ内の src を、本番プロジェクトの src がある階層へコピーし、フォルダを結合して同名ファイルを上書きします（2ファイル）。src を丸ごと削除・置換したり、src/src に入れたりしないでください。
04まで反映してからクリーンビルド・再配置・アプリ再起動を行います。

修正内容と従来の確認結果は「修正内容・検証結果（元資料）.md」を参照してください。元資料にある旧フォルダ名は、親フォルダの対応表に読み替えてください。
ソースのファイル名・配置・内容は検証済みパッケージと同一です。