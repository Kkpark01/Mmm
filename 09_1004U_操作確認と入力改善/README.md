> **2026-09-15：最新版src (4)へ反映済み。** 2ファイルともSHA256完全一致を確認しました。再反映は不要です。

# 09：MCM1004U 取引先見積内容

> **再確認で見つかった3点を修正しました。** 孤立データ整理・変更なし登録の抑止・9999年内の期間追加に対応しています。[修正と検証の記録](../../検証記録/review_20260914/1004U/VB準拠修正結果.md)を参照してください。09を既に反映した場合も、今回の2ファイルを反映してください。

基準：2026-09-14受領のsrc (3).zip（E1931F34）。反映対象は既存の **2ファイル**です。新規アプリファイル・DB定義変更はありません。

- `src/main/resources/templates/mcm1004u/index.html`
- `src/main/java/com/daifuku/mcm/service/Mcm1004uService.java`

## 08との反映順

**08と09はどちらから反映しても構いません。** 08は1003UのJava・HTML・CSS、09は1004UのHTML・Serviceで、同じファイルは含まれていません。受領503ファイルへ08→09／09→08の順で上書きした場合の全パス・SHA256を照合し、最終結果が同一であることを確認しました。09単独でも動作しますが、1003Uからの戻り先修正には08も反映してください。

このフォルダ内のsrcを、対象プロジェクトのsrcに同じ相対パスで上書きし、通常のビルド・再起動を行ってください。旧04〜07を重ねる必要はありません。会社側で同じHTMLが追加修正されている場合は、差分一覧.jsonの元SHA256と照合して統合してください。

## 今回の修正と配置確認

- 登録・前後の期間調整・管理者ロック解除・最新情報の再読み込みを、他画面と同じ確認ダイアログへ統一。キャンセル／Escapeで処理を中止し、確認中の重複送信を抑止。
- 数値欄で有効な指数表記（例：1e3＝1000）や末尾ゼロを含む入力でも、画面の金額を正しく再計算。サーバーの数値検証・金額計算処理は既存処理を使用。
- VBの上部4行、期間・金額欄、単価表、点検表、左下のタブ追加・右下の登録を照合し、既存の配置を維持。タイトルの外周8px・高さ40px・文字18px、通常行24px、横スクロールと固定列を確認。
- ロック解除ボタンは管理者権限に応じた表示です。比較画像のWeb側は通常ユーザーの表示です。
- VBのMCM_CLEAN_PAC.SP_TMと同じ9条件・同じ順で不要データを整理。対象はVB同様に取引先見積全体です。Webでは権限・入力・変更有無の確認後に保存と同じトランザクション内で実行し、保存失敗時は削除も取り消します。未変更・入力エラーの場合は整理を実行しません。
- 編集可能な値に変更がなければ「データに変更がありません。」で終了し、更新日時を書き換えません。選択タブの変更や数値の末尾ゼロはデータ変更と扱いません。
- 9999年内の翌期間追加を許可し、9999/12/31から10000年になる追加のみ拒否します。

## 確認結果

- 最新srcの実Service・Controller・Thymeleaf、ローカルH2とVB由来の4ビューを使い、1004Uの33項目成功。読込・保存・金額計算・チェック解除・期間追加／連動・入力拒否・競合検出・ロールバック・権限・ロック解除・1007照会・戻り先を確認。
- Chrome操作29項目成功。実保存と再読込・入力保持・照会ウィンドウ・固定列・1024／1264／1440／1920pxの表示・共通確認／キャンセル・送信回数を確認。
- ロック解除／再読み込み／変更なし登録の追加ブラウザ確認10項目成功。キャンセル時の未保存値保持、承認後の再読込、変更なしの通知と日時維持、1003への戻り先レスポンスを確認。
- VB準拠修正の追加15項目成功。不要データ整理の各条件、正常な別見積8表の維持、保存失敗時の整理ロールバック、変更なし・9999年境界を確認。合計87項目成功（33＋29＋10＋15）。
- Javaは最新受領386ファイルをコンパイルした検証環境を使用し、今回のServiceを再コンパイル。08のJavaを含む環境でも確認しています。

本番SQL Server・実ログインを含む会社環境での最終確認は未実施です。1007は照会先としての表示・引継ぎまでで、画面全体の個別修正は次の対象です。

[VB／Web比較画像](../../検証記録/review_20260914/1004U/画面別比較/1004U/基幹機能/1004U_比較セット.png) ／ [確認ダイアログ](../../検証記録/review_20260914/1004U/画面別比較/1004U/基幹機能/1004U_確認ダイアログ.png)

VB側はDesignerからの配置再構成で、実機スクリーンショットではありません。検証記録は `検証記録/review_20260914/1004U` に保存しています。最新受領のmcm-web/srcは503ファイルすべて変更していません。
