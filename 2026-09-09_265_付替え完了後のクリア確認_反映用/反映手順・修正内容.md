## #265 追加修正：付替え完了後のクリア確認

VBの Mcm0021uScreen.vb（MSG_0098呼出し）と CPMessage.xml に合わせ、付替え実行前と実行後の確認を分けています。

1. 実行前：「付替えしてもよろしいですか？」。キャンセルなら更新しません。
2. 正常完了後：「付替えを完了しました。付替え元、付替え先をクリアしてよろしいですか？」。
   - OK：付替え元・付替え先の設定、一覧、ブランド候補をクリアします。検索条件と検索結果は保持します。
   - キャンセル：元・先の設定を保持します。付替え自体は完了済みです。

クリア処理ではDBを更新せず、付替えを再実行しません。完了後の確認は1回だけ表示し、リロードでは再表示しません。キャンセル時の再実行防止は従来どおり維持します。古い確認のトークンでは新しく設定した元・先をクリアできません。

## 反映手順

前提：#258・#261・#265（5ファイル）→ #257（3ファイル）の順で反映済みであること。

このフォルダの src をプロジェクトの src にマージし、同名の2ファイルを上書きしてください。先のパッケージがまだなら、上記の順に反映してから今回分を最後に反映します。

- src/main/java/com/daifuku/mcm/controller/Mcm0021uController.java
- src/main/resources/templates/mcm0021u/index.html

Javaの変更があるため、クリーンビルド・アプリの完全停止からの再起動・ブラウザー強制再読み込みが必要です。今回分は#257の修正を含むControllerとテンプレートを基準にしており、#257より後に上書きしてください。#236とMCM2004Uの19ファイル版とは重複しません。

## 確認結果

追加36項目（サーバー22・ブラウザー14）と、既存43項目（#257が16・#258/#261/#265が27）の計79項目が成功しました。実行前キャンセル、完了後OK／キャンセル、表示順・文言、再読込、クリア時DB再更新なし、不正・古いトークン、セッション切れ、検索結果保持を確認しています。OKとキャンセル後の画面画像も目視確認済みです。

検証は実Controller・Service・Thymeleaf・JavaScriptとモックDBを使っています。実DBでの確認は未実施です。既存の契約不整合時の警告分岐（MSG_0132）は今回変更していません。

詳細・ログ・画像：作業ルートの review_20260909/fix0265-clear。元ソースと先の修正パッケージは変更していません。

