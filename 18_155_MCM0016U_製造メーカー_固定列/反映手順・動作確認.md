# #155 MCM0016U 製造メーカーマスタ

2026-09-17時点のmcm-web/srcを基準とする修正です。

## 反映

このフォルダ内のsrcを対象プロジェクトへコピーし、同じパスのindex.htmlを上書きしてください。反映前に対象ファイルを退避し、通常のビルド・再起動後にブラウザーを再読み込みしてください。

変更ファイル：src/main/resources/templates/mcm0016u/index.html

共通JSやCSSの追加・上書きはありません。今回の17〜24は反映パスが重複せず、単独でも順不同でも反映可能です。元のmcm-web/src自体は更新していません。

## 修正

横スクロールの固定列、列幅変更後の位置追従、見出しの前後関係、追加行のhidden項目による列ずれ、削除行の透けを対策。固定列数（行選択列を含む）：{"makerTable":4,"koseiTable":3}。登録処理や入力イベントは変更していません。

## 利用環境での確認手順

1. データのある状態で画面を開き、通常行・必須項目の色を確認します。
2. 横スクロールして左の固定列が残り、背後の文字が透けないことを確認します。
3. No列や固定列を広げる・狭める操作後も、列の重なりや隙間がないことを確認します。
4. 縦スクロールし、固定列見出しが隠れないことを確認します。
5. 行追加・選択・削除を行い、追加行も同じ列で固定され、選択色・削除色・取消線が維持されることを確認します。保存する場合は検証用データを使ってください。
6. 子一覧がある画面では親行を切り替え、子一覧を表示し直して確認します。
7. ウィンドウ幅変更後も同様に確認します。

検証結果の詳細は親フォルダの「155_反映案内.md」を参照してください。実DBを使う登録・再表示は未検証です。
