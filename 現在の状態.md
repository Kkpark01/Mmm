# 現在の反映用フォルダ（2026-09-09更新）

プラント付替え（MCM0021U）は **2026-09-09_プラント付替え_順番に反映** の中の **01 → 02 → 03 → 04** を順番に使用してください。

- 01_258・261・265_基本修正：src 5ファイル
- 02_257_検索後の自動選択解除：src 3ファイル
- 03_265_付替え完了後のクリア確認：src 2ファイル
- 04_265_付替え後の検索一覧更新：src 1ファイル

各srcを同じプロジェクトへ結合・上書きし、04完了後にクリーンビルド・再配置・完全停止からの再起動を行います。詳細は同フォルダ内の「最初に読む_反映手順.md」を参照してください。
プラント付替えの旧名フォルダは保管用です。重ねて反映する必要はありません。

別画面の修正は従来どおり次のフォルダを使用します。

- mcm-web_fixed_#236_納入機器明細選定_検索0件：1ファイル
- mcm-web_fixed_MCM2004U_VB準拠_画面・機能修正：19ファイル

---

## 以下は過去の作業記録

以下に記載された旧フォルダ名と反映順は過去の記録です。現在の反映には上記の案内を優先してください。

# 修正コードの整理状況

2026-09-08、最新版の `mcm-web/src` と照合し、役割を終えた以下の5フォルダと古い `済.zip` を削除しました。

- #184 担当者名100文字
- #191 プラント付替え 第2段階
- 納入機器マスタ 機能修正
- 納入機器マスタ 500エラー修正 20260908
- 納入機器明細選定 機能修正

14UのControllerとJavaScriptには、最新版ソース側で#247・#253の追加修正が入っています。それ以外の修正ファイルは改行コード・BOMを除いて最新版と一致しています。

## 今回の反映用パッケージと反映順序

1. `mcm-web_fixed_#258_#261_#265_プラント付替え` の `src`（5ファイル）を反映。
2. 続けて `mcm-web_fixed_#257_プラント付替え_追加修正` の `src`（3ファイル）を反映。
3. `mcm-web_fixed_#236_納入機器明細選定_検索0件` の `src`（1ファイル）を反映。
4. `mcm-web_fixed_MCM2004U_VB準拠_画面・機能修正` の `src`（19ファイル：既存18＋新規JS1）を反映。
5. `2026-09-09_265_付替え完了後のクリア確認_反映用` の `src`（2ファイル）を反映。1→2の後に、この5を最後に上書きしてください。

先の3件が反映済みなら、1は不要です。#257は先の3件を基準にした差分パッケージです。1と2を逆順に上書きすると#257の修正が戻ります。#236は14UのControllerのみで、1・2の21U修正とはファイルが重複しません。4も1～3とファイル重複がありません。5は1・2のControllerとテンプレートを追加上書きするため、5を最後に反映します。5フォルダとも現時点で反映用として使用します。

`mcm-web_fixed_#258_#261_#265_プラント付替え` に、最新版 `mcm-web/src` を基準とした追加修正を保存しています。中のsrcをマージして5ファイルを上書きし、クリーンビルド・再配置・アプリ再起動を行ってください。

#265の構成IDはVB版の全件移動・一部分割方式で、利用者に仕様確認済みです。3件の要件はコードと49項目のローカルテスト上で充足と判断しています。本番DBでの検証は未実施です。詳細はパッケージ内の反映手順を参照してください。

#257は検索結果の自動選択を解除し、未選択時に「付替え元が１件もチェックされていません。」を表示します。追加31項目と先の3件の回帰49項目、合計80項目を確認済みです。DBはモックによる検証です。各パッケージ内の手順書を参照し、反映後にクリーンビルド・完全停止からの再起動を行ってください。

#236は既存の検索0件ダイアログの本文を「検索結果が１件も存在しません。」に統一します。実Controller・テンプレート・JavaScriptを使い、テスト用検索データで32項目と画面目視を確認済みです。実DBでの確認は未実施です。

MCM2004Uはメニュー右上の見積関連「②」の画面です。VBの検索・一覧配置、初期チェック、メッセージ、遷移・受け渡し、破棄・行削除を修正し、色・文字・帯を他Web画面に合わせました。2007Uへの契約作成の受け渡し修正と遷移先の不具合修正を含む16ファイルで、③のMCM1003Uは含みません。ローカル93項目と画面目視を確認済みですが、DBはモックです。詳細と反映手順は同パッケージ内にあります。

削除前の内容は `review_20260908/修正コード整理_*` 内の「削除前バックアップ.zip」に退避し、全ファイルのハッシュ一致を確認しています。復旧用であり、本番への上書き用ではありません。

2004Uの遷移先と行削除を追加確認し、2001U・2002Uの表示／選定時の不具合を修正しました。同じパッケージを16ファイル版へ更新済みです。追加50項目成功、これまでの合計143項目。実DB未検証の範囲はパッケージ内の説明を参照してください。


MCM2004Uパッケージの最新版は19ファイルです。通常操作・目視で見つかった2006Uの重なり、遷移先から戻る導線を修正しました。追加24項目成功、これまでの合計167項目。src全体を反映してください。実DB未検証です。


2026-09-09：今回の#265クリア確認修正は『2026-09-09_265_付替え完了後のクリア確認_反映用』に分かりやすく再保存しました。src内2ファイルは検証済み修正版とハッシュ一致です。旧名フォルダにも同じ修正が残っていますが、今後の反映にはこの新フォルダを使用してください。
