# #155【全マスタ固定列】修正差分メモ

更新日：2026-09-07

## 2026-09-07 表示重なりの再修正

取引先マスタ・システム定義・店舗マスタ・保守会社マスタでの重なり／行選択列への文字の透けの報告を受け、7画面を再確認した。前回の66件は固定位置の数値確認が中心で、描画順・透明度・実際の行構造を十分に検証できていなかった。

- 固定見出しのz-indexが7で、通常見出し（取引先マスタでは8）の下に隠れる設定だった。固定見出しを20に変更。
- システム定義の列幅変更初期化が見出しをposition:relativeに上書きしていた。stickyを維持するよう修正。
- 取引先マスタの追加行などで、tr直下のhidden項目までnth-childが数えるため、固定対象がずれる構造だった。固定セルと必須背景の判定をnth-of-typeに変更。
- 削除行の行／セルのopacityが0.6で背後の文字が透けていた。固定列対象テーブルでは行とセルを不透明にし、削除色と取消線を維持。
- 固定セルの内容をセル内に収め、隣列へのはみ出しを防止。

今回の変更は既存のindex.html 7件。mcm-fixed-columns.jsは前回版のままで引き続き必要。追加ファイルは増えていない。

### 再検証

実際のテンプレートの行・入力欄・CSS・列幅変更処理を使い、Chromeで7画面11グリッドを検証。表示値はダミーで、Thymeleafやアプリ全体は実行していない。

- 初期表示／横スクロール／No列を75px拡大／縦スクロールの44ケースが通過。
- 通常行・選択行・セル選択・削除行・行先頭にhidden項目がある行について、固定位置、stickyの維持、不透明な背景、他セルに覆われていないことを確認。
- 前回の列幅追従テスト66件も再実行して通過。インラインJavaScript構文も確認。
- 報告の4画面を含むスクロール画像を目視確認。

検証コード・結果・画像：MCM/review_20260907/fix155-overlap/（check.cjs、after-result.json、after-画面ID.png）。修正前HTMLは同フォルダのbefore-sourceに保存。

利用者環境での解消確認は未実施。下記srcを再反映し、アプリのビルド・配備手順に従って更新すること。

## 課題と修正内容

VB版のFrozen列に対応する横スクロール固定列をJava版に追加する課題。

前回の修正資材では、固定列のleftを初期列幅の累積値でハードコードしていた。既存の列幅ドラッグ処理がcolの幅を変更してもleftが更新されず、固定列が重なる／隙間ができる問題があった。

今回、各固定列のleftをCSS変数参照に変更し、実際に描画された見出しセルの幅を累積して固定位置を更新するJavaScriptを追加した。

## 対象画面・固定列数

選択列を含む、左端からの固定列数。

| 画面 | グリッド | 固定列数 |
|---|---|---|
| MCM0016U 製造メーカーマスタ | makerTable / koseiTable | 4 / 3 |
| MCM0017U 取引先マスタ | parentGrid / child1Grid / child2Grid | 5 / 4 / 6 |
| MCM0018U 取扱機器マスタ | atsukaikikiGrid / koseiGrid | 6 / 3 |
| MCM0019U 機器分類マスタ | kikibunruiGrid | 4 |
| MCM0022U 店舗マスタ | tenpoGrid | 4 |
| MCM0023U 保守会社マスタ | hoshugaishaGrid | 4 |
| MCM0024U システムマスタ | systemTable | 4 |

MCM0015Uは前回から対象資材に含まれていないため、今回も変更していない。

## 実装

- 各tableにdata-mcm-fixed-columnsで固定列数を設定する。
- 各固定列のCSSのleftをvar(--mcm-fixed-left-N, 初期位置)に置き換える。
- mcm-fixed-columns.jsが見出しセルのgetBoundingClientRect().widthを測り、累積値をtableのCSS変数へ設定する。小数ピクセルも保持する。
- ResizeObserverでtableと固定列見出しの幅を監視する。既存の列幅ドラッグ、初期幅調整、ウィンドウリサイズ、子グリッドの表示切替に追従する。
- display:noneのグリッドは幅0の測定結果を反映せず、再表示時に再計算する。
- CSS変数は子孫へ継承されるため、追加・再描画されたデータ行にも適用される。
- ResizeObserverがない環境ではcolgroupのstyle変更とwindow.resizeを監視する。この互換経路は今回のChrome検証の対象外。
- 固定見出しを通常見出しより前面に描画する。固定列境界の影と必須項目背景色は維持する。

## 反映ファイル（合計8件）

1. src/main/resources/templates/mcm0016u/index.html
2. src/main/resources/templates/mcm0017u/index.html
3. src/main/resources/templates/mcm0018u/index.html
4. src/main/resources/templates/mcm0019u/index.html
5. src/main/resources/templates/mcm0022u/index.html
6. src/main/resources/templates/mcm0023u/index.html
7. src/main/resources/templates/mcm0024u/index.html
8. src/main/resources/static/js/mcm-fixed-columns.js（新規）

HTML7件と新規JavaScriptをセットで反映すること。HTMLだけでは幅変更への追従処理が動作しない。

既存のcommon.js・Javaコードは変更していない。列幅変更処理はシステム定義の見出しposition指定のみ修正した。mcm-web本体にはまだ反映していない。

## 2026-09-06時点の検証結果（位置計算の確認）

実際の各画面のCSS、colgroup、見出し、列幅ドラッグ処理を使い、データ行をダミー生成したChromeの検証ページで確認。

11グリッド×以下6ケース＝66件通過。各ケースで見出し・全データ行の固定位置が見出し幅の累積値と一致することを検証した。

- 初期表示。
- No列の幅をドラッグ処理で60px拡大。
- 横スクロール後。
- データ行を追加した後。
- グリッドを非表示→再表示した後。
- ウィンドウリサイズ後（既存の初期幅調整処理が実行された後）。

変更したテンプレートのインラインJavaScript構文、ブラウザ実行時のJavaScriptエラーも確認。製造メーカーマスタの幅変更・スクロール後の画像を目視確認した。

検証ファイル：

- MCM/review_20260906/fix155/browser-check.cjs
- MCM/review_20260906/fix155/test.log
- MCM/review_20260906/fix155/mcm0016u-drag-scroll.png

## 未確認の範囲

MCMアプリを起動した状態でのThymeleaf描画、実DBデータでの操作、利用者環境での表示確認は未実施。2026-09-07の検証では、テンプレートの入力欄を含む検証ページで重なりを確認した。
反映後は、各画面の固定列を拡大・縮小して横スクロールし、列の重なりや隙間がないこと、および親子グリッド切替・行追加・入力操作を確認する。
