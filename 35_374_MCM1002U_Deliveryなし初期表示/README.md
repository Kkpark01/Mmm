# 課題374 MCM1002U Deliveryなしの直接アクセス

**2026-09-25受領srcと一致（改行差・BOM除外）。再反映不要です。以下は作成時の記録。**

2026-09-24受領src.zipを基準。元srcは未変更です。

## 修正と反映

既存1ファイル `src/main/java/com/daifuku/mcm/controller/Mcm1002uController.java` を同じ場所へ上書きし、ビルド・再起動してください。30～34と対象ファイルの重複はありません。

原因：plantIdなしの場合、Deliveryの有無より先に保存済みScreen1を復元していました。

変更：plantIdもDeliveryもない場合、古いScreen1/Screen2フォームをセッションから除き、新しい空フォームを返します。機器一覧とヘッダーは空、更新不可、戻り先はMCM1001Uです。対象外のセッション情報は変更しません。

Deliveryがある通常遷移とScreen2の「前へ」は維持。plantId付きのMCM1001Uからの遷移も維持します。VB Mcm1002u1Screen.vb 39行付近はDeliveryありの場合に連携値を設定。今回は課題の期待動作を優先し、Deliveryなしで古いフォームを使わないようにしました。

## 検証

- 最新受領src＋35のJava387ファイルがコンパイル成功。
- 実Controller＋セッション代替・Serviceスタブの5項目成功：保存済みフォームだけ残る場合の初期化、新規セッション、Delivery付き戻りでのフォーム保持、Delivery付き新規表示、plantId付き遷移による旧情報置換。
- 記録：検証記録/review_20260924/374/result.txt、DirectAccessTest.java、compile-result.json。
- 実HTTP/Thymeleaf描画・ログイン/ログアウト・実DBの結合確認は未実施。Jev呼出しなし。

## 適用範囲と実機確認

タブを閉じても通常セッションは失効しません。Deliveryが残っている場合のURL直打ちを自動判別する変更ではありません（その場合は正規遷移や「前へ」と区別できないため）。この修正は課題の前提であるDeliveryなしを対象とします。

会社環境でDeliveryなしの `/mcm1002u` が空表示になり、前回データが出ないことを確認してください。ログアウト後の未認証アクセスは既存認証に従い、再ログイン後の新規セッションでも確認してください。正常な1001/1003/1008からの遷移とScreen2から「前へ」で選択状態を維持することも確認してください。
