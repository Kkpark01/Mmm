# 07：0010Uへのメニュー再入場のみ修正

2026-09-14受領の最新src（04反映済み、05・06未反映）向けです。**今回反映するのはこの07の1ファイルだけです。05・06は保留のままです。**

## 変更内容

`MenuController.java` のメニュー表示処理に、0010Uの検索条件 `mcm0010u_search_form` を破棄する処理を1行追加しました。

- 検索→メニュー→0010U：検索条件と一覧が空の初期表示になり、前回条件による4つの自動検索を行いません。
- 詳細画面→0010U（メニューを経由しない）：従来どおり検索条件を保持して再検索します。
- 検索ボタン：従来どおりです。

一覧幅・フォント・配置・HTML/CSS・検索SQL・件数・保存処理の変更はありません。05の表示軽量化や画面改修、旧06の05用セッション処理は取り込んでいません。

## 反映方法

1. 会社の最新srcをバックアップします。
2. このフォルダの `src/main/java/com/daifuku/mcm/controller/MenuController.java` を、会社のsrcの同じ場所へ上書きします。
3. ビルド・再起動し、検索→メニュー→0010Uで条件・一覧が空になること、再度の検索が動くこと、詳細から直接戻ると条件が残ることを確認します。

**旧06と同じファイルを対象とするため、06と一緒に反映しないでください。** 04は最新版に取り込み済みで再反映不要、05も不要です。会社でMenuControllerを追加修正した場合は、ファイル全体を上書きせず差分を統合してください。07を適用した会社srcが正になります。

## 確認結果

05・06を含めず、最新srcの385 Javaファイルをコンパイル後、07のMenuControllerを再コンパイルしました。実Controller＋MockMvcの11項目が成功。再入場時の検索Service呼出回数は修正前4回→修正後0回。直接戻る場合は4回を維持。通常検索、ログイン情報・他画面の状態、従来の子画面クリア、未ログイン時のログイン遷移を確認しました。

Serviceは呼出回数を測るモックです。SQL Serverでの実測時間、ブラウザー描画、詳細画面全体の再検証は今回行っていません。受領srcそのものと04・05・06のアプリコードは保持し、この07に差分を保存しています。

検証記録：`検証記録/review_20260914/menu0010/`。元ファイルと修正版のSHA256は `差分一覧.json` を参照してください。
