#257 プラント付替え検索：検索後の自動選択を解除
================================================

## 反映順序

1. 先に `mcm-web_fixed_#258_#261_#265_プラント付替え/src` を本番プロジェクトへマージし、5ファイルを上書きしてください。反映済みならこの操作は不要です。
2. 続いて、このフォルダの `src` を同じプロジェクトへマージし、3ファイルを上書きしてください。プロジェクト直下の既存 `src` に重ねます。既存の `src` フォルダ全体を削除・置換する操作はしないでください。
3. アプリを完全停止し、クリーンビルドした成果物を配置して再起動してください。ブラウザーも再読み込みし、再ログインしてください。

このパッケージは先の3チケットを反映した状態に追加する差分です。単独では先の3チケットの全ファイルは揃いません。今回の3ファイルには先の修正も含めてあります。反映後に古いパッケージを上書きすると、#257の修正が戻るため、上記の順序を守ってください。

## 上書き対象（ファイル名の変更なし）

- `src/main/java/com/daifuku/mcm/constants/Mcm0021uConstants.java`
- `src/main/java/com/daifuku/mcm/controller/Mcm0021uController.java`
- `src/main/resources/templates/mcm0021u/index.html`

先のパッケージのServiceとRepositoryは、そのまま使用します。DBの構造変更はありません。作業元の `mcm-web/src` は変更していません。

## 修正内容

- サーバーの検索結果保存処理、HTMLの先頭行の選択指定、JavaScriptの先頭行への自動選択を解除しました。
- 検索結果がある状態で利用者が選択せず「付替え元に設定」を押すと、次の文言を表示して処理を中断します。数字の「１」は全角です。

  `付替え元が１件もチェックされていません。`

- ブラウザーとサーバーの両方で未選択を確認します。未選択では付替え元を新たに設定せず、すでに設定している付替え元も変更しません。
- 現在のWeb画面の検索結果は行クリックによる選択方式です。この操作方式を維持し、利用者が行を選択した場合だけ設定できるようにしました。設定後の画面復帰では明示的に選択した行を維持しますが、再検索では未選択に戻します。
- 検索未実施・検索結果なしなどの#191の判定、機器情報なしの#258の判定、#261・#265の処理は維持しています。
- 下段の設定済み付替え元一覧にある移動対象チェックボックスは、今回の検索結果の自動選択とは別の項目です。

## 確認結果

- #257：Controller・テンプレートの16項目、Chromeブラウザーの15項目が成功。
- 先の#258・#261・#265：Controller・Service等27項目、トランザクション・SQL等11項目、Chromeブラウザー11項目の回帰確認が成功。
- 合計80項目。未選択時の文言完全一致・送信抑止、明示選択後の設定、再検索時の選択解除、先の修正の維持を確認しました。

検証記録は `review_20260908/fix0257` に保存しています。DB接続部分はモックを使用しており、実DB・本番環境での動作確認は未実施です。
